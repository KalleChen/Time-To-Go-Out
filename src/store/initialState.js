import { initialState as allSpotState } from '../reducers/allSpotReducer'
import { initialState as citySpotState } from '../reducers/cityReducer'
import { initialState as uiState } from '../reducers/uiReducer'

const initialState = {
  allSpotState,
  citySpotState,
  uiState
}

export default initialState
